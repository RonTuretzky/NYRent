# Etherform minimum setup (build + test + fmt) per
# https://github.com/BreadchainCoop/etherform — see docs/OPERATIONS.md.
# Deploys are intentionally NOT enabled on PR: Gnosis mainnet deploys are a
# manual operator action with the key only in local env (SPEC §6, §8).
name: CI/CD
on: [push, pull_request]

permissions:
  contents: read
  pull-requests: write

concurrency:
  group: cicd-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  cicd:
    # Temporarily via the RonTuretzky fork: upstream pins foundry-toolchain v1.8.0,
    # which breaks on the Rust foundryup (fork bumps it to v1.9.1).
    uses: RonTuretzky/etherform/.github/workflows/_foundry-cicd.yml@main
